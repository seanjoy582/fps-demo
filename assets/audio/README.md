# Audio

Gunfire, reload, hit, hurt, and ambient sounds are generated at runtime with Web Audio in `js/audio.js`.

This keeps the demo self-contained while leaving this folder ready for future licensed `.wav` or `.mp3` replacements.
